# Solutions

Public solution links are intentionally omitted from this hard build to avoid fingerprinting the application lineage.

Use private training material or deployment-specific notes instead.
